#include "types.h"
#include "stat.h"
#include "user.h"
//#include <stdio.h>

int main(int argc, char *argv[])
{
    int param = atoi(argv[1]);
    info(param);
    //printf("Info returned: %d", num);
    exit();
}